# We are going to assume for now that the engine is not going to be 
# accessible by the user, all configuration will be provided by the 
# larger app. The larger app will give the user the option to change 
# some db configuration for where the internal measurement data is kept
# and maybe whether to persist it?